server:
  port: 8040
spring:
  application:
    name: consumer-feign
eureka:
  client:
    register-with-eureka: true
    service-url:
      defauleZone: http://127.0.0.1:8761/eureka/
  instance:
    prefer-ip-address: true

feign:
  hystrix:
    enabled: true