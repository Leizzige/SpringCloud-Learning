spring:
  cloud:
    config:
      name: application
      profile: dev
      label: master
      discovery:
        enabled: true
        service-id: git-config-server

eureka:
  client:
    service-url:
      defauleZone: http://127.0.0.1:8761/eureka/
