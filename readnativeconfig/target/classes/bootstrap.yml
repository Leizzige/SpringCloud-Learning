spring:
  application:
    name: configclient
  profiles:
    active: dev
  cloud:
    config:
      uri: http://127.0.0.1:8762
      fail-fast: true

eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka/